    
    <div class="col-md-6 col-md-offset-3">
    <div class="jumbotron">
    <div class="text-center">
        <div class="row">
        <div class="col-md-8">
            <div class="profile">
                <img src="https://picsum.photos/200" >
            </div>
        </div>
        <div class="text-left">
        <div class="col-md-4">
            <h1><%=@user.name%></h1>
            <div class="lead"><%=@user.bio%></div>
            <div>Age: <%=@user.age%></div>

            <%if @user.teams != nil && @user == current_user%>
                <br><h4><%= link_to "Team(s):", teams_path%> </h4>
                <ul><h5><%@user_teams.each do |team| %>
                    <li><%= link_to team.name, team_path(team)%></li>
                <%end%></h5></ul>
                    <%= link_to "Join A Team", new_userteam_path%><br>
    
            <%elsif @user.teams != nil%>
                <br><h4>Team(s):</h4>
                <ul><h5><%@user_teams.each do |team| %>
                    <li><%= team.name%></li>
                <%end%></h5></ul>
            <%end%>

            <%if current_user == @user%>
                <br>
                <%= link_to "View Current Tasks", tasks_path%><br>
                <%=link_to "Edit Profile", edit_user_path(@user)%><br>
                <%=link_to "Delete", user_path(@user), :method => :delete, data: {confirm: "Are you sure you want to delete your profile? We'd hate to see you go!"}%><br>
            <%end%>
        </div>
        </div>
    </div>
        



    </div>
    </div>
